# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files (gerçek değerler — repo'ya girmez)
.env
.env.local
.env*.local
functions/.env
functions/.env.local
# .env.example commit edilir (şablon)

# typescript
*.tsbuildinfo

app-example

# generated native folders
/ios
/android
node_modules
serviceAccountKey.json
# Firebase Admin SDK & Legacy Secrets (ASLA commit etme — tam yetkili gizli anahtar)
serviceAccountKey.json
functions/serviceAccountKey.json
*serviceAccount*.json
*service-account*.json
*firebase-adminsdk*.json
*-firebase-adminsdk-*.json
_migrate/.env.legacy
expo.log
functions/firestore-debug.log

# Firebase client config — API key dahil. Public repo'da görünmemeli.
# Lokal kopyaları geliştiriciler kendi tutar; CI/CD env'den enjekte eder.
google-services.json
GoogleService-Info.plist

# EAS build profile — Firebase env değişkenleri açık.
# eas.example.json şablon olarak commit'lenir.
eas.json

# Lokal/geliştirme test invoker'ları (gerekirse lokal tut)
functions/test_invoke_*.js